# Core service
fastapi>=0.110
uvicorn>=0.29
numpy>=1.24
pydantic>=2.0

# SehatLine deep-learning model (PyTorch). CPU build is fine:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Optional (only if you also train the legacy GBR/MLP baselines in train_model.py)
scikit-learn>=1.3
joblib>=1.3
