{
  "apiBaseUrl": "https://REPLACE-WITH-AZURE-URL.azurewebsites.net",
  "note": "The app reads this at startup to find the backend. When the backend host/URL changes, edit apiBaseUrl here and push — installed apps pick it up, no rebuild. Do not delete."
}
